export function calculatePoints(isCorrect, questionPoints) {
  // TODO: correct = full points, wrong = 0
}

export function calculateLeaderboard(participants) {
  // TODO: sort by totalScore descending, assign ranks (handle ties)
}
