import { broadcastToRoom } from "../utils/broadcast.js";

export async function handleBroadcastLeaderboard(ws, data) {
  // TODO: fetch participants sorted by totalScore, broadcast to room
}

export async function handleBroadcastQuestionResult(ws, data) {
  // TODO: fetch question result, broadcast option distribution to room
}
